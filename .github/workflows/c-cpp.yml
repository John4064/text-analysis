name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    
    #After Checked out
    - name: Run a one-line script
      run: echo Branch is Checked Out!
      
    #- name: configure
    #  run: ./configure
    
    - name: make
      run: make -f makefile.txt
    
    - name: make check
      run: make check
    
    - name: make distcheck
      run: make distcheck
